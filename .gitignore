# Табло задачи — локальное состояние, у каждой машины своё.
# Сами хуки и .claude/settings.json, наоборот, ИДУТ в git:
# без них слой принуждения при переносе проекта молча исчезнет.
.claude/task-state.json

# Python и Django — локальное, в git не идёт
.venv/
__pycache__/
*.pyc
db.sqlite3
.env
staticfiles/
media/
.pytest_cache/

# Служебные файлы запусков: расписание Celery Beat и метаданные
# установки пакета — создаются сами, в git не идут
celerybeat-schedule*
*.egg-info/
