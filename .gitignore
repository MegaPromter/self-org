# Табло задачи — локальное состояние, у каждой машины своё.
# Сами хуки и .claude/settings.json, наоборот, ИДУТ в git:
# без них слой принуждения при переносе проекта молча исчезнет.
.claude/task-state.json
